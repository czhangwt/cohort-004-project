# Sandcastle (`@ai-hero/sandcastle`)

Used for running Claude Code in a Docker sandbox to implement features from a PRD + plan. The entrypoint is `.sandcastle/main.ts`.

This is not part of the app itself — it's the AI agent orchestration layer for the learning cohort.
