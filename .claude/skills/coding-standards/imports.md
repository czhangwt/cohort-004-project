---
name: coding-standards--imports
description: Use ~/ import alias for anything inside /app, never relative imports. Use when adding or fixing import statements.
---

# Imports

Use `~/*` import alias for anything inside `/app`. Don't use relative imports like `../../lib/utils`, use `~/lib/utils` instead.
